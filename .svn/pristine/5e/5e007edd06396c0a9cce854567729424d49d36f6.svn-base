class AddLastTaggedCategorizeGeolocalizeToSources < ActiveRecord::Migration
  def change
    add_column :sources, :last_tagged, :datetime
    add_column :sources, :last_categorize, :datetime
    add_column :sources, :last_geolocalize, :datetime   
  end
end
